let typed = new Typed(".text",{
    strings:["Frontend Developer", "Bloger", "Web Developer"],
    typeSpeed:100,
    backSpeed:100,
    backDelay:1000,
    loop:true
})
// let client = true
// let discount = client ? '25%' : '5%';
// console.log('Discount:', discount);